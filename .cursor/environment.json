{
  "name": "Apex Trading Platform",
  "install": "cd trading-platform/backend && pip3 install -r requirements.txt && mkdir -p data && cd ../dashboard && npm ci && chmod +x ../scripts/*.sh",
  "terminals": [
    {
      "name": "trading-platform",
      "command": "trading-platform/scripts/start-with-tunnel.sh",
      "description": "Backend + Cloudflare tunnel for 24/7 paper trading (interim until Render deploy)"
    }
  ],
  "ports": [
    { "name": "api", "port": 8000 },
    { "name": "dashboard", "port": 3000 }
  ]
}
