{
  "name": "Apex Trading Platform",
  "install": "cd trading-platform/backend && pip3 install -r requirements.txt && mkdir -p data && cd ../dashboard && npm ci && chmod +x ../scripts/*.sh",
  "terminals": [
    {
      "name": "trading-platform",
      "command": "trading-platform/scripts/start-full-platform.sh",
      "description": "Backend + dashboard + Cloudflare tunnels for 24/7 public CRM"
    }
  ],
  "ports": [
    { "name": "api", "port": 8000 },
    { "name": "dashboard", "port": 3000 }
  ],
  "secrets": [
    "RENDER_API_KEY",
    "VERCEL_TOKEN",
    "RENDER_DEPLOY_HOOK",
    "TRADINGVIEW_WEBHOOK_SECRET"
  ]
}
